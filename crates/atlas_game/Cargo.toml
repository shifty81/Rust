[package]
name    = "atlas_game"
version = "0.1.0"
edition = "2021"
description = "Gameplay systems and plugins — the actual game logic, shared between runtime and PIE."

[dependencies]
bevy     = { workspace = true }
atlas_core  = { workspace = true }
atlas_scene = { workspace = true }
