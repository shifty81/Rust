[package]
name    = "atlas_render"
version = "0.1.0"
edition = "2021"
description = "Render setup, pipelines, and game-view configuration."

[dependencies]
bevy    = { workspace = true }
atlas_core = { workspace = true }
